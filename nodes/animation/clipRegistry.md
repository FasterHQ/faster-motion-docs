# Clip Registry

**Type:** `clipRegistry`  
**Category:** animation  
**Context:** Shared — works in both DOM and canvas graphs  

F342: graph-native publisher of AnimationClip references by id. One dynamic output port per clip (clip_${id}) carries the live AnimationClip reference. Replaces imperative TPN/OPE bind(clip) seams with port-routed clip identity.

## Inputs

_No inputs._

## Outputs

_No outputs._

## Parameters

_No configurable parameters._
